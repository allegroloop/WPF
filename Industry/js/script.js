// Assignment Expressions - Industry
// Calculate the average pay per day in a given month

var month = 4; // I used 22 as the number of working weeks in a month
var week = 5 // Defining how many day are in one work week
var payChecks = [738, 875, 650, 754]; // An arbitrary list of weekly paychecks
var total = payChecks[0] + payChecks[1] + payChecks[2] + payChecks[3]; //add all the paychecks using and array

var average
